# Efficiency / Deployment Cost

CPU-only throughput was measured on CT&T Test04 for three repeated runs. Timing separates feature extraction, window aggregation, and classifier inference. Feature extraction includes CSV parsing because this is the current reproducible repository path.
