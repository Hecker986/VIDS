# Raw-window + Same Classifier Control

This experiment isolates representation from classifier choice. Both rows use GradientBoosting, W100, validation-F1 thresholding, and the same CT&T train/test split. Raw-window features exclude same-ID residuals; GRAIN features include the causal same-ID timing and payload residual fields.
